yarn cache list [--pattern]
Yarn сохраняет каждый пакет в глобальном кэше в каталоге вашего пользователя в файловой системе. yarn cache list выведет список всех кэшированных пакетов.

yarn cache list --pattern <pattern> будет распечатан каждый кэшированный пакет, соответствующий предоставленному шаблону.

Примеры:

yarn cache list --pattern gulp
yarn cache list --pattern "gulp|grunt"
yarn cache list --pattern "gulp-(match|newer)"
yarn cache dir
При запуске yarn cache dir будет выведен путь к глобальному кэшу yarn, который в данный момент хранится.

yarn cache clean [<module_name...>]
Выполнение этой команды очистит глобальный кэш. Он будет заполнен заново при следующем запуске yarn или yarn install. Кроме того, вы можете указать один или несколько пакетов, которые хотите очистить.

Измените путь к кэшу для yarn
Установите cache-folder значение config для настройки каталога кэша.

yarn config set cache-folder <path>
Вы также можете указать каталог кэша с помощью флага --cache-folder:

yarn <command> --cache-folder <path>
Вы также можете указать каталог кэша с помощью переменной окружения YARN_CACHE_FOLDER:

YARN_CACHE_FOLDER=<path> yarn <command>
Yarn также будет уважать cache=<path> in .npmrc на предмет совместимости с npm.


FramePack может обрабатывать очень большое количество кадров с моделями 13B даже на графических процессорах ноутбуков.

FramePack можно обучать с гораздо большим размером партии, аналогичным размеру партии при обучении диффузии изображений.

Распространение видео, но по ощущениям похоже на распространение изображений.
